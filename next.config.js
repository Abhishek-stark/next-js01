/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/Abhishek-stark/next-js01",
    experimental: {
        appDir: true,
    },
};

module.exports = nextConfig;